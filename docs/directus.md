# Directus: схема, автоматизация, соглашения

Общий глоссарий проекта (сокращения MKS/MLS, термин «событие», различие works/perfs) — теперь в [README.md](../README.md#terminology).

Этот документ описывает то, что нельзя восстановить, просто прочитав код репозитория — потому что оно живёт в базе Directus (`db.sch.com.ru`), а не в git. Читать перед тем, как менять схему, заводить Flow или разбираться, почему что-то на сайте выглядит так, а не иначе.

Доступ для приложения Hugo при сборке сайта — токен в `config/_default/params.json` (не в git, см. `.gitignore`), роль **ReadOnly Policy** в Directus, доступ на чтение к перечисленным ниже коллекциям.

## Коллекции

- **`works`** (Песня) — `id` (строка, совпадает с именем файла текста), `incipit`, `name` (по конвенции — со строчной буквы, часто пусто), `sort` (всегда заполнено, по нему построен рабочий поиск/сортировка в каталоге — надёжнее, чем `name`), `fname`, `fincipit`, `hidden`, `date` (год создания; отрицательное значение используем для неточной датировки — при построении URL вида `texts/<year>/<id>.html` в шаблонах обязательно брать `math.Abs`, иначе получится путь со знаком «минус», не совпадающий с реальной папкой), `author`/`music` (для песен не за своим авторством — «чужих»), `is_popurri`, `is_poem` (произведение целиком является стихотворением без музыки; **не путать с `perfs.is_poem`** ниже — это разные поля на разных коллекциях с похожим, но не идентичным смыслом), `description`, **`edit`** (год последней большой редакции — см. раздел «Редакции» ниже), **`date_created`** (заполняется автоматически Directus только для новых записей, у старых — `null`; используется для даты в меню «Каталог песен»).
- **`variants`** (Вариант) — `work_id` → `works`, `date`, `is_last_auth`, `url`. Коллекция задумана для словесных вариантов исполнения (через `perfs.variants_id`), **не** путаем варианты исполнений с полноценными редакциями текста — в коллекции нет поля для самого текста.
- **`perfs`** (Исполнение) — `id` (собирается автоматически, см. Flow «Auto perf id»), `works_id` → `works`, `variants_id` → `variants`, `events_id` → `events`, `number` (порядок в программе концерта, проставляется тем же Flow'ом), `is_premiere` (первое исполнение), `is_first`/`is_last` (первая/последняя песня всего концерта), `is_bis` (на бис), `is_first_after_break`/`is_last_before_break` (граница антракта), `take` (булево — дубль/повтор попытки), `is_poem` (стихи), `note` (замечание по исполнению, не авторское), `mls_instrument` (не булево — на чём играет МЛС (Михаил Леонидович Стародубцев) в этом исполнении), набор булевых про инструменты/дуэт (`with_mls` (в дуэте ли с МЛС), `plays_mls_guitar` (играет ли МЛС на гитаре), `plays_mls_keyboards` (играет ли МЛС на клавишах), `has_mls_voice` (поёт ли МЛС), `has_synth` (участвует ли в исполнении синтезатор как секвенсор, то есть синтезирован ли аккомпанемент, это параметр для студийных исполнений, в отличие от sings_with_minus -- редчайших живых исполнений под минус на концерте), `is_ensemble` (для исполнений не в дуэте с МЛС, но в ансамбле с другими музыкантами), `is_pseudo_duet` (даб щербаковского голоса на студийных исполнениях, для дабов гитары, видимо, параметр сочтён несущественным и их не учитывает), `mks_no_guitar` (MKS (МКЩ, Михаил Константинович Щербаков) не играет на гитаре, редко, но бывает), `mks_no_voice` (Щербаков не поёт, редко, но встречается, напр. на мелодийных композициях в "Целом лете" второго издания), `sings_with_minus` (поёт на концерте под минус, очень редко), `sings_mls_only` (очень редко, когда МЛС поёт щербаковскую песню, а Щербаков -- нет), `sings_incomplete` (для неполностью спетых песен), `performers` (M2M-связь с коллекцией `performers` ниже; на 2026-08-21 заполнена почти нигде — 5 записей из 13534, остальные участники дуэтов пока не перенесены из старых текстовых заметок/`note`).
- **`events`** (Событие) — `id` (см. соглашение по именам ниже), `status` (`published`/`draft`/`archived`/**`cancelled`**; **`draft` не значит «будущий/анонсированный концерт»** — проверено на живых данных: все 33 события со статусом `draft` оказались старыми `is_studio=true` записями с датами в прошлом, а реальные ещё не состоявшиеся концерты имеют статус `published` с датой в будущем. Различать «прошёл/не прошёл концерт» нужно строго по `date`, не по `status` — см. `directus-concerts.html` ниже), `date`, **`year`/`dw`** (год и день недели, автозаполняются Flow'ом «Auto year» из `date`), `country` (страна) /`town` (город) / `state` (штат или регион) / `place` (дом, зал, площадка), `day_evening` (дневной или вечерний концерт), `is_studio` (студийная звукозапись), `is_home` (квартирник, домашний концерт), `comment` (краткий комментарий) / `description` (более развёрнутое описание) / `author_intro` (авторское вступительное слово), `set_id` → `sets`, **`songs_performed`** (Alias-поле, обратная связь к `perfs` через `perfs.events_id`, ничего не хранит физически — см. ниже).
- **`sets`** (Коллекция исполнений/альбом) — `id`, `is_album` (является ли опубликованным альбомом), `title`, `year` (год публикации), `comment`, `album_img`, `ignore_in_catalogue` (скрывать для большей упорядоченности в каталоге песен, как напр. первое издание Балаган 2).
- **`sets_entries`** — join-таблица `perfs` ↔ `sets` (`perfs_id`, `sets_id`, `number` (порядковый номер исполнения в коллекции)).
- **`announces`** (аннонс концерта или объявление). `event` (M2O → `events`, необязательное — при создании можно создать событие прямо из формы анонса), `text` (своя формулировка, чтобы не использовать шаблон аннонса по умолчанию), `link` (ссылка на билеты), `publish_date`/`expiry_date` (окно видимости), `status` (публикуется только анонс со статусом `published`).
- **`performers`** (Исполнители) — `id`, `name` (краткое, напр. «Ю. Ким»), `fullname` (полное имя). Всего **3 записи**: `MKS`, `MLS` (см. сокращения в начале файла) и `JCK` (Ю. Ким — участвует в редких дуэтных исполнениях). Читается по ReadOnly-токену без проблем (проверено 2026-08-21) — открытый вопрос про отсутствие доступа снят, см. ниже.

## Flows (Settings → Flows)

Оба — Event Hook, тип **Filter** (не Action — важно, Filter выполняется до записи и может подменить payload). У обоих в настройках триггера обязательно выставлен **Response Body → Last Operation** — без этого результат операций тихо отбрасывается и в базу ничего не попадает, при этом лог покажет, что скрипт отработал верно. Это реальный баг, на который мы напоролись и потеряли время — если новый Flow «не работает», но в логе всё верно, первым делом проверять именно это поле.

- **Auto year** (`events`, `items.create`+`items.update`): один Run Script — если `date` заполнена, вычисляет `year` и `dw` (короткие строчные — `вс`,`пн`,`вт`,`ср`,`чт`,`пт`,`сб`) и не трогает их, если `date` не задана (для приблизительно датированных концертов).
- **Auto perf id** (`perfs`, `items.create`): цепочка из трёх операций — Run Script `set_id` (если `id` не введён вручную, собирает `events_id + "_" + works_id`) → Read Data `existing` (все `perfs` этого `events_id`, сортировка `-number`, лимит 1) → Run Script (берёт результат `set_id`, если `number` не введён вручную — ставит `max(existing.number) + 1`). Проверено на пакетном добавлении всего концерта разом (25 песен за одно сохранение события) — номера идут строго по порядку, коллизий нет.

## Соглашения по именам

- **`id` события**: `<L|s><YYYY-MM-DD>[-N]` — `L` = живой концерт (Live), `s` = студия; дата или `XX`, если неизвестна; суффикс `-N` — случай нескольких событий в один день (напр. дневной/вечерний концерт).
- **`id` исполнения**: `<events_id>_<works_id>` — собирается автоматически скриптом в directus, вручную заполняется редко.
- **Архивная (замененная) редакция песни**: см. следующий раздел.

## Редакции песен (`works.edit`) и архивные версии

Когда песня получает **большую редакцию** (несколько строк и больше — не путать с `variants`, которые про мелкие словесные разночтения):

1. `works.id` в Directus **не меняется никогда** — одна запись на песню на всю историю. Проставляется `edit` = год редакции.
2. Актуальный текст — обычный `content/texts/<год-редакции>/<id>.md` + `assets/texts/<id>.txt`, `id` в front matter совпадает с `works.id`.
3. Архивный (старый) текст — файл `assets/texts/<id>0.txt` (суффикс `0`; исторически встречался и вариант «заменить последний символ на 0» для соглашения имён файлов в формате 8.3, но для новых случаев — добавлять `0` перед расширением, единообразия ради).
4. **Нужна ли отдельная страница `content/texts/<год-создания>/<id>.md` под архив** — зависит от того, был ли этот URL уже опубликован (реально ушло коммит и на GitHub Pages):
   - **Не был опубликован** — страницу не заводить вообще (исторический пример: `ljudisux`/`ljudisu0.txt` и ещё 5 подобных — старый текст лежит только как файл, без страницы).
   - **Уже был опубликован** (реальный пример — «Бутафор», редакция 2022→2025) — старую страницу нужно сохранить (не удалять!), поправив в её front matter `params.id` на суффиксованный (`butafor0`), плюс добавить:
     - `"sitemap": {"disable": true}` в корне front matter — страница пропадает из `sitemap.xml`;
     - `params.newerEdition: {"year": ..., "id": ...}` — `songpage.html` сам добавит `<meta name="robots" content="noindex, follow">` и баннер-ссылку на актуальную редакцию (CSS класс `.notice`, копирует горизонтальный паддинг `.view-container` и вертикальный отступ `#settings`).

Каталог (`catalogue.html`) сам ведёт на `texts/{{ works.edit | default date }}/{{ id }}.html` — маршрутизация в шаблоне, реализация 2025 года.

## Сторона Hugo в связке с БД Directus

- `layouts/partials/directus-announces.html` + `layouts/partials/announces.html` — тянут активные (по `publish_date`/`expiry_date`/`status`) анонсы, при отменённом связанном событии (`event.status = cancelled`) автоматически подменяют текст на сообщение об отмене; подмешивают и старый ручной массив `content/news.md → params.announce` (не сломан, просто не единственный источник теперь). Используются и в `header.html` (баннер на главной), и в `news.html`.
- `layouts/partials/directus-lastmod.html` — дата у пункта «Каталог песен» в меню теперь берётся из `max(works.date_created)`, а не из захардкоженного `lastmod` в `content/catalogue.md`. Подключается через новый параметр меню `dynamicLastmod: "works"` в `hugo.json`.
- `layouts/partials/directus-concerts.html` — список на `concerts.html` фильтруется на стороне запроса: `is_studio=false`, `status != cancelled`, `date <= сегодня` (см. выше про `events.status`). Будущие анонсированные концерты и отменённые в публичный список не попадают, при этом сам факт «концерт ещё не состоялся» определяется исключительно датой.
- `works.is_poem` — используется в двух местах: `catalogue.html` добавляет к записи пометку «(стихотворение)»; `songpage.html` (через новый партиал `layouts/partials/directus-work.html`, точечный запрос по `id`) при `is_poem=true` заменяет блок `#settings` на упрощённый (только «текст»/«исходник», без аккордовых кнопок). Подробности по каждому полю front matter, которые это затрагивает — в `docs/frontmatter.md`.
- `hugo.json → caches.getresource.maxAge: "10m"`. По умолчанию Hugo кэширует `resources.GetRemote` (все `directus-*.html` partial'ы) **бессрочно** — это несколько раз давало устаревшие данные в этой сессии. При тестировании изменений в Directus использовать `hugo --ignoreCache`.
- `archetypes/announces.md` (не `.html` — расширение архетипа должно совпадать с расширением создаваемого файла, иначе `hugo new` его не находит).

## Деплой изменений, затронувших только БД Directus

`.github/workflows/static.yml` — GitHub Actions, собирает Hugo **в одноразовом раннере** (не в репозитории) и публикует через `actions/deploy-pages` — это отдельный артефакт, не связанный с git-историей. Триггеры сейчас: `push` на `master` и ручной **`workflow_dispatch`** (кнопка «Run workflow» во вкладке Actions на GitHub — не нужен ни git, ни Hugo локально).

**План (не реализовано)**: Flow с операцией Webhook/Request на сохранение в `announces`/`events`/`perfs`/`works`, дёргающий `POST /repos/zlochevsky/shcherbakov/actions/workflows/static.yml/dispatches` — чтобы контент-админ, никогда не трогающий git, просто нажимал Save в Directus, а сайт пересобирался сам.

## Открытые вопросы / тупики (не исследовать заново без новых вводных)

- **M2O-виджет выбора песни** (`perfs.works_id` и подобные) — в Directus 11.3.5 нет более лёгкого inline-варианта интерфейса; клик по полю всегда открывает полноэкранную панель, плюс нужен клик по лупе для активации поиска. Решение — кастомный interface-extension `directus-extensions/setlist-editor/` (собран и задеплоен 2026-08-17, `Enabled` в Settings → Extensions): рендерится на alias-поле `events.songs_performed`, даёт строку ввода с автокомплитом по `works` и сразу пишет `perfs` с FK на песню, не открывая панель. Поиск — клиентский (см. следующий пункт), а не через API-фильтр.
- **Табличный layout для массового дозаполнения булевых параметров** (`perfs`/`works`) — протестированы оба кандидата на живой базе (2026-08-18):
- `directus-extension-super-table` — **рабочий**, это и есть рекомендованное решение. Обязательно включить в Layout Options **Edit Mode → Enable inline editing** (и **Direct Boolean Toggle** для чекбоксов) — без этого выглядит как обычная штатная Table и кажется бесполезным. Это layout уровня коллекции (Content → perfs/works → переключить Layout), применяется только к верхнеуровневому списку, **не** работают внутри O2M-панели, вложенной в форму концерта — там нужен кастомный extension `setlist-editor` (см. выше).
  - Собранные пакеты (без локальной сборки, публикуются уже готовыми) лежат в `directus-extensions/spreadsheet-layout/` и `directus-extensions/super-table/` в репозитории — на случай переустановки.
- **Регистронезависимый поиск по кириллице был сломан на уровне SQL** — sqlite'шный `LOWER()`/`UPPER()` не умел корректно сворачивать регистр кириллицы, из-за чего `_icontains`/`_istarts_with` на нелатинских полях не находили записи независимо от регистра запроса. Это и стало основной причиной миграции БД на PostgreSQL (2026-08-19). После миграции проверено вживую через API (2026-08-21): баг не воспроизводится, `_icontains` по кириллице работает корректно.
- **`_contains`/`_icontains` не экранируют SQL LIKE-спецсимволы `_` и `%` в значении фильтра** — обнаружено 2026-08-18 через поиск super-table: запрос `_ye` находил `...krasnye`, потому что `_` в LIKE-паттерне означает «любой один символ», а не буквальное подчёркивание (`_ye` совпало с «nye» в «krasnye»). Подтверждено напрямую через API. Из-за этого поиск по произвольному тексту, содержащему `_`, ведёт себя непредсказуемо — сравнительно безобидно для проставления галочек, но стоит помнить при любом будущем поиске/фильтрации по `perfs.id`/`works.id` (там `_` — обычный разделитель в самих id, `<events_id>_<works_id>`).
- **`perfs.performers`** — уже не открытый вопрос в прежнем виде: доступ к `performers` есть, поле уже M2M (проверено 2026-08-21). Осталось только перенести старые текстовые записи об участниках дуэтов из `note` в саму связь — сделано пока для 5 исполнений из 13534, идея с чекбоксами MKS/MLS/JCK + кнопкой добавить в форме `perfs` не реализована.

## Роли и кто как обновляет сайт (план)

- **Мейнтейнер + «продвинутый» контент-админ по музыке** — работают файлами и git-коммитами, существующий `push`-триггер их полностью покрывает.
- **Контент-админ анонсов** — только Directus, без git; нужен вебхук-триггер (см. «Деплой» выше), пока не собран.
- **Будущий администратор истории концертов** (фото + исполнения прошедших концертов) — рекомендовано хранить фото через встроенное файловое хранилище Directus, а не как файлы в git, чтобы этот человек тоже не касался git вообще и попадал под тот же вебхук-триггер, что и анонсы.
