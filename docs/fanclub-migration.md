# План: перенос раздела «Фан-клуб» (и «Архива») с legacy-сайта

Инструкция для следующей сессии. Обновлено 2026-09-24 (прежняя версия была
написана до переноса хаба и утверждала, что локальной копии исходников нет —
это неверно). Держите при себе, но не воспринимайте как техзадание слово в
слово: сверяйтесь с владельцем на развилках (§7). Контекст решений о границе
«витрина / клуб» и навигации — `docs/site-structure-analysis.md`; **концепция
навигации не принята и перенос её не блокирует**.

## 1. Статус на 2026-09-24

Замер: `python3 scripts/check-links.py` — **106 битых внутренних целей** (145
ссылок-источников), из них `fans` 29, `Images` 11, `Parodies` 4, `SCH2` 2.
Метрика прогресса: эти числа должны падать.

| Область | legacy (NAS) | Состояние | Что делать |
|---|---|---|---|
| `fans/` хаб `index` | 1 | перенесён (`content/fans/fans-index.md`, url `/fans/index.html`) | ссылки на ~22 дочерние страницы битые → Волна 1; финальная проверка → Волна 7 |
| `fans/smirnov`, `fans/Demiurg`, `fans/utf` (3), `crossword`, `krossvord` | 46 файлов в `content/fans/` (на NAS от `smirnov` только 5 страниц — остальное взято с внешнего зеркала) | перенесены | — |
| `fans/*.html` прочие | 55 не перенесено | нет | Волны 1–2 (для клуба реально ~44, см. ниже) |
| **Переводы** (`fans/utf/{esperanto,german,hebrew,ukrain,francais}`, корневые дубли `esperanto`, `hebrew`, `ukrain`, `English/` 60 страниц) | 8 + 60 | нет | **вынесены из клуба в отдельный слой** — §3а; не переносить, пока не решены URL и модель |
| `Parodies/` | 63 файла, 348 КБ | нет | Волна 3 |
| `SCH2/` | 4 файла, 68 КБ | нет | Волна 4 (публикуем как на старом сайте) |
| `Archive/` (**витрина**, «Концерты», не клуб) | 6 файлов, ~46 КБ | **перенесён 2026-09-25** (Волна 8, см. ниже) | — |
| `Images/` | 79 страниц | **страницы объединены**, переносить не нужно | Волна 5 (проверка + 11 ссылок из лога) |
| Словарь (`kom.html`/`kom2.html`) | 284 статьи | в Directus **14 из 284** (пилот) | отдельная линия, `docs/slovar-migration.md`, Волна 6 |

Из 55 непереносённых `fans/*.html` исключить: `erofeev.html`, `erofeev2.html`
(уже есть `content/Praises/erofeev*.md`), `kom2.html` (это Словарь → Directus) и
8 страниц **переводов** (5 в `fans/utf/` + 3 корневых дубля; `fans/utf/*` были на
legacy каноническими — UTF-8 ради символов, которых нет в KOI8-R; решение
владельца 2026-09-24: переводы — слой основного сайта, а не клуб). Остаётся
**~44 страницы клуба**. Не всё в `utf/` и в корне — переводы: `utf/crocodile`
(«Крокодилиада», цикл пародий, в шапке `[Пародии] [Фан-Клуб]`) и `kogolublu`
(разбор рифм) — клуб; `brahms` (Рюккерт), `horatius` (Гораций), `mueller`
(Мюллер, «Gute Nacht»), `oncetran` — **исходные и параллельные тексты песен**
(близки к Комментариям), остаются в клубе, помечены на проверку.
Общий объём непереносённого HTML в `fans/` ≈ 1,0 МБ; самые крупные: `katrin`
(138 КБ, «Пьеса»), `bestiar` (80), `utf/crocodile` (49), `chgk` (47), `utf/esperanto`
(41), `herbary` (35), `utf/hebrew` (28), `potop` (24), `utf/ukrain` (21), `discussion` (18).

### «Картинки»: гипотеза владельца подтверждена

Раздел был реструктурирован (мелкие страницы объединены по годам, см.
`docs/photo-pages-merging.md`; хвост про «Иллюстрации/Вокруг дисков» там
устарел — они уже сделаны). Проверка 2026-09-24: **76 из 79 legacy-страниц
целиком присутствуют на новых страницах** (498 из 527 ссылок на фото, 94%);
среди 498 `<img>` legacy-страниц файла нет в `static/Images` только у двух.
Страницы переносить **не надо**.

**Баннеры (сделано 2026-09-24):** две legacy-страницы, лежавшие в `static/Banners/`
в KOI8-R, перенесены в контент как **одна** страница подраздела «Картинок»:
`/Images/banners.html` (`content/Images/banners.md`; отдельную страницу Фуксмана
владелец удалил как ненужную, его миниатюры встроены в основную). HTML5 без
таблиц и презентационных атрибутов; старые `/Banners/index.html` и
`/Banners/fooksman.html` — `aliases` на эту страницу (Hugo сам кладёт в них
`<link rel="canonical">`). Шаблон `images-page` теперь выдаёт canonical и умеет
необязательные `params.bgColor` (у баннеров `#FEFEEF`, как в legacy) и ссылку на
родителя (`parentUrl`/`parentTitle`, сейчас никем не используется). Файлы баннеров
остались в `static/Banners/` (на них ссылаются страницы `Archive/` и, возможно,
чужие сайты). Блок «Баннеры» и якоря на всех блоках добавлены на страницу «Картинки»
(`photos`, `ill`, `afish` — имена из ссылок legacy; `around-disks`, `banners` — новые).
Не используется на странице: `sch_sf1.jpg`, `fooksman/style.css`.

## 2. Источники

- **NAS:** `/mnt/nas/diskh/sch/sch.com.ru/www/` — `fans/`, `Parodies/`, `SCH2/`,
  `Images/`. Каталог `scherbakov/` внутри — зеркальная копия, не использовать.
- Кодировка: корневые страницы `fans/` (`file` показывает `iso-8859-1` — это
  заглушка) на деле KOI8-R (или cp1251) — определять по файлу, как в плейбуке
  «Отзывов»; `fans/utf/*` — UTF-8.
- Нет на NAS: `fans/utf/covers.html` (на неё ссылается хаб) — скачать с внешнего
  зеркала (`lambda.mkshch.com/fans/utf/covers.html`, `blackalpinist.com`), как
  делали для `fans/smirnov`/`Demiurg`.
- Процесс конвертации — **плейбук `praises-migration-playbook.md` в памяти
  проекта** (`~/.claude/projects/-home-sergius-schugo/memory/`), с поправками §5.

## 3. Порядок работ

**Волна 0 — подготовка (небольшая, делать первой).**
1. Умолчание `backUrl` в `layouts/miscellaneous/praises-article.html` по префиксу
   URL (`fans/` → `fans/index.html`, `Praises/` → `praises.html`, `FOM/` →
   `FOM/index.html`, `SCH2/`, `Parodies/` → свои хабы), чтобы новые страницы не
   получали «Отзывы» по умолчанию. Заодно лечит 18 существующих страниц (FOM 13,
   `fans/utf` 3, Смирнов 1, Демиург 1).
2. Договориться о дублях переводов (§7) и об имени хаба Парадий: файл
   `content/Parodies/parodies-index.md` с `url: /Parodies/index.html` (ловушка
   page bundle: файл `index.md` в каталоге «проглатывает» соседей).

**Волна 1 — 24 страницы клуба, на которые уже ссылается перенесённое** (хаб
`fans/index`, лог, `crossword`, `smirnov/sch_2`, `Images/illustr*`; ещё 5 страниц
переводов из `fans/utf/` тоже на эту роль претендуют — они ушли в §3а). Батчи по
~6–8 страниц, по размеру ровно, тяжёлые отдельно:
- 1a `fans/utf/crocodile` (49 КБ, «Крокодилиада» — пародии; при переносе учесть
  шапку `[Пародии] [Фан-Клуб]`) + `covers` с внешнего зеркала (на NAS нет; по
  старому плану «каверы» — уточнить, каверы это или переводы);
- 1b Андрианов/Вильмс/обсуждения: `andrianov`, `andr2`, `andr-rusalka`,
  `andr-rusalka-d`, `potop`, `discussion`, `memory`, `memo/schneider`;
- 1c игры и «сборники»: `bestiar`, `herbary`, `chgk`, `chgkizb`, `izvraty`,
  `comix/comix` (+ картинки комикса);
- 1d прочее: `katrin` (отдельно, 138 КБ), `fedenkova`, `julia`, `parabola`,
  `rechisty`, `suzy1`, `zhandarov`, `answer` (ответы к кроссворду).
  После волны — **включить «Фан-клуб» в меню главной**: в `hugo.json`
  (`indexfootermenu`) `pageRef: "fans/fans-index.md"` вместо `"fans/index.html"`
  (сейчас там мёртвая `<a>` без `href`).

**Волна 2 — оставшиеся ~21 страница `fans/`, в основном ≤5 КБ:** `bes`, `bom-bram`,
`brahms`, `elfy`, `eslinado`, `horatius`, `kogolublu`, `lirika`, `mandlsh2`,
`mandlsht`, `metafora`, `mueller`, `nikst-glaza`, `oncetran`, `polkovnik`,
`rakety`, `rubcov-doroga`, `sardanapal`, `shuty`, `szs-intrm4`, `memo/podborka`.
`brahms`, `horatius`, `mueller`, `oncetran` — исходные/параллельные тексты песен
(Рюккерт, Гораций, Мюллер); не переводы Щербакова, поэтому остаются здесь.

**Волна 3 — `Parodies/`:** хаб + 62 страницы (348 КБ, короткий текст), батчи по
~12; хаб последним. На `Parodies/index.html` ссылаются 3 страницы, на `ziplakov`,
`mumu`, `greenland` — записи лога.

**Волна 4 — `SCH2/`:** `index`, `103-e`, `gb/index`, `profiles`. Решение
владельца 2026-09-24: **публикуем как на старом сайте** (гостевая книга и
профили респондентов лежали открыто тридцать лет); актуальность данных не
проверять и не вычищать.

**§3а. Слой переводов — вынесен из клуба (решение владельца 2026-09-24).**
Переводы — не фан-клуб, а новый слой основного сайта; владелец рассматривает
минимальные языковые версии вокруг серий переведённых песен, которые сообщество
сможет развивать. Что относится сюда: `fans/utf/{esperanto,german,hebrew,ukrain,
francais}`, корневые старые версии `esperanto`, `hebrew`, `ukrain`, весь
`English/` (60 страниц: ~58 переводов одной песни на страницу + два указателя
`index`, `trans`). **Не переносить под `fans/`** (по URL это «клуб» для проверки
границы и хаба) — сначала решить URL-схему и модель, см.
`docs/site-structure-analysis.md` §6.

**Волна 5 — «Картинки»: проверка и ссылки.**
- Сверить три страницы: `drubin` (26 из 52 картинок не найдено на новых страницах,
  вероятно, пары превью/оригинал), `boston03` (2 из 3, одного файла нет в
  `static/Images`), `index` (хаб против `gallery.md`).
- 11 битых ссылок ведёт **только `log.html`** (записи Directus `log_entries`) на
  старые страницы: `bardru1`, `cdri1005`, `cupert08`, `dkmgu04`, `drubin`,
  `gnezdo05`, `minsk05`, `minsk88`, `morist08`, `shale05`, `zhukovsk`. Чинить
  **данными в Directus** (перенаправить на объединённые страницы с якорем), не
  в репозитории. Соответствие «старая страница → новая» вычисляется по
  пересечению файлов фото.
- Алиасы для 76 старых URL — только если владелец захочет (внешних ссылок на
  эти пути, скорее всего, мало).

**Волна 6 — Словарь → Directus** (`docs/slovar-migration.md`): 274 статьи
осталось; после — редирект `kom2.html` → `kom.html`. Независимая линия, можно
параллельно.

**Волна 7 — финал хаба:** `fans/index` — все дочерние ссылки живые,
`python3 scripts/check-links.py --section fans` пуст, якоря проверены.

**Волна 8 — `Archive/` (витрина, а не клуб; небольшая, можно делать рано и независимо
от клуба).** Добавлено 2026-09-24 по просьбе владельца.
- **Что это:** 6 страниц, ~46 КБ, KOI8-R. `index` (26 КБ) — «Архив концертов, гастролей и
  прочих событий: расписания, координаты организаторов»: по сути старые новости по
  годам (с 2010 назад). Отчёты о гастролях: `america97`, `america99` (по-английски,
  «M.Scherbakov's American Tour…»; встраивают 8–9 баннеров `../Banners/*.jpg` —
  это картинки, файлы остаются в `static/Banners/`), `europe2000`, `usa2001`,
  `usa2003` (2–6 КБ).
- **Куда:** `content/Archive/`, URL те же (`/Archive/index.html`, `/Archive/europe2000.html`…),
  алиасы не нужны. Хаб назвать `archive-index.md` с `url: /Archive/index.html`
  (ловушка page bundle). Шаблон — по образцу Новостей/`praises-article` (уточнить у
  владельца). `check-separation.py` считает `/Archive/*` **витриной**.
- **Уже сломано на живом сайте:** на `Archive/index.html` ведут `about`, `log`, `news`;
  на `europe2000` — `Images/banners.html`. Плюс баг регистра в
  `layouts/miscellaneous/news.html` (строки 17 и 47): `href="archive/index.html"` —
  на GitHub Pages (регистрозависимо) ссылка не работает, надо `Archive/index.html`; там же
  подпись «Aрхив» набрана с латинской «A» — заменить на кириллическую. Исправить сразу,
  не дожидаясь миграции.
- **Границы (решить до переноса):** страницы архива ссылаются в клуб — на `Banners/
  index.html` (`index`, `europe2000`, `usa2001`, заодно «[Афиши] [Баннеры]» в их
  шапках), на `Images/index.html#afish` (те же три) и на `Praises/xazagpre.html`
  (`index`). По прецеденту Био — афиши и баннеры не упоминать; по прецеденту Новостей —
  допустимо, если внизу и немного. Варианты: убрать шапочные ссылки и внутренние или
  внести в `ALLOW`. Новые ссылки писать сразу на `Images/banners.html`, а не на
  `Banners/index.html`.
- **Ещё не существующие цели:** `Tapes/01|02|03|2000.html` (раздел «Кассеты», 11
  страниц, отдельная задача) и `kniga_pamyati.html` (Память); остальные 14 внутренних
  целей (диски `Disks/*` и др.) уже есть. Пока `Tapes/` не перенесён — ссылки остаются
  битыми или временно снимаются (решить).
- **Вопрос модели:** переносить как статические страницы (дёшево, сохраняет URL) или
  позже влить записи индекса в Directus `events` (поля `comment`/`description`)
  и в Каталог концертов; по умолчанию — статические страницы, слияние — отдельная идея.
- **Проверка:** `check-links.py --section Archive` должен опустеть, `check-separation.py` —
  нарушения только по решённым выше исключениям.

**Волна 8 — выполнено 2026-09-25.** Перенесены `content/Archive/{archive-index,america97,
america99,europe2000,usa2001,usa2003}.md` (шаблон `layouts/miscellaneous/archive-page.html`,
фон `#FEFEEF`, ссылки `#006600`, как на старом сайте; шапка: Главная / Новости / Архив / Лог /
Афиши / Баннеры). Правила кода — как в Отзывах (нижний регистр тегов, ` - `/`&ndash;` →
`&nbsp;&mdash;`, `"…"` → «…», зеркало `blackalpinist.com/scherbakov/…` → внутренние ссылки,
комментарии и `mailto` сохранены; английская `america97` без замены дефисов). Список
`usa2003` — по левому краю; таблица `europe2000` — блоки `<p>` по строке таблицы, первая колонка
жирным с `<br>`, как в `usa2001`. Заодно, как цели ссылок Архива, перенесены: `Tapes/01|02|03|2000`
(шаблон `tape-page.html`, обложки `static/Tapes/izbran1_*.jpg`; id песен сопоставлены с новыми:
`krym1→krym10`, `pustye→pustyjeb`, `vechnoe→vechnoes`, `nazimnej0→nazimne0`, `akoekto→akoektop`,
`monologi#feb→fevral`) и `kniga_pamyati.html` (шаблон `praises-article`, назад — на
`fans/smirnov/`). Не перенесены: `Tapes/index.html` (хаб «Кассеты», ссылки на него из `about`,
`bio` остаются битыми; кассеты 04–08 и `ozon` не тронуты), ссылка «[Кассеты]» из старой шапки
кассет опущена.
Решение владельца 2026-09-25: из шапки Архива ссылки на афиши/баннеры убраны; `Praises/annin`,
`Praises/xazagpre`, `kniga_pamyati` из текста индекса внесены в `ALLOW`. `Tapes/` (шаблон, статьи,
наполнение БД) — отдельная задача на потом.

Вне этого плана (отдельные задачи): слой переводов (§3а, включая `English/`),
`Tapes` (остальные 7: `index`, `04–08`, `ozon`), `Books` (`vv`, `dz`, `13d` и др. — 10 целей),
`Disks` (`krym`, `else`, `other`), страницы годов `texts/*` и `htmtexts/*`.

## 4. Как проверять каждую партию

- `python3 scripts/check-links.py --section fans` — цели должны уходить из списка;
- `python3 scripts/check-separation.py` — новых нарушений быть не должно (клуб
  ссылается на витрину свободно, обратное — нет);
- сверка «источник ↔ md» по блокам, e-mail и HTML-комментариям; счёт слов
  **не ловит** потерянный последний блок — смотреть его отдельно;
- битые `#якоря` (файл есть, `name=`/`id=` нет; `#top` игнорировать) — скрипта
  в репозитории нет, писать разовый;
- `hugo`-сборки агентам **не запускать**: у владельца работает `hugo server`
  (`--renderToDisk`), он сам обновляет `public/`; проверки читают `public/`.

## 5. Правила переноса (дополнения к плейбуку «Отзывов»)

- Шаблон `praises-article` + явный `backUrl` (после Волны 0 — по умолчанию по
  разделу). Ссылки только **относительные** (не `/…` от корня — сайт живёт под
  `/shcherbakov/`); `htmtexts/<y>/<id>.html` → `texts/<y>/<id>.html`; количество
  `../` считать от реального места нового файла.
- Сохранять **всё**: e-mail, HTML-комментарии, блоки-«не влияющие на содержание»
  (агенты уже однажды убрали их «из соображений приватности» — владелец просил
  вернуть). Опечатки исходника сохранять.
- Хабы называть `<раздел>-index.md` с `url: …/index.html`.
- Строка с 4+ пробелами или табом перед блочным HTML-тегом в Markdown становится
  code block — проверять `grep -Pn '^(\t|    )' content/<раздел>/*.md`.
- Приведение регистра тегов — quote-aware (значения атрибутов не трогать),
  отдельным коммитом.
- Ассеты: у каждого `<img>`/файла должен быть реальный файл в `static/` (сначала
  искать в `static/Images/`, `static/fans/`, потом на NAS `www/Images/`).
- Git: `public/` в `.gitignore`, но несколько файлов из него закоммичены давно
  (`public/Images/index.html`, `afish.html`, `public/sitemap.xml`) — не трогать;
  добавлять только явные исходные пути.
- Клуб → витрина ссылаться можно; нельзя ставить в статьи новые ссылки «вперёд»
  на клубные страницы из витрины (проверяет `check-separation.py`).

## 6. Известное состояние, которое стоит поправить по пути

- Мёртвая `<a>` «Фан-клуб» в подвале главной (Волна 1, см. выше); «English» тоже.
- В `docs/photo-pages-merging.md` хвост «ещё не тронуто» устарел.
- Прежний дизайн-намёк «хаб клуба — имитация таблиц как на главной, фон — цвет
  оригинальной таблицы» **не реализован**: хаб перенесён в стиле
  `praises-article`; уточнить, актуален ли он.

## 7. Вопросы владельцу

**Отвечено 2026-09-24:**
1. Переводы: `fans/utf/*` были каноническими на legacy (UTF-8 ради символов вне
   KOI8-R). Теперь переводы — **слой основного сайта, не клуб**; концепция
   (языковые версии, выбор языка в верхнем меню) не оформлена — §3а и
   `docs/site-structure-analysis.md` §6.
2. `SCH2/gb`, `SCH2/profiles`: публикуем как на старом сайте, актуальность не
   проверяем.

**Открыто (спросить в начале сессии):**
3. Актуален ли старый дизайн хаба клуба (§6) или оставить как есть?
4. Порядок волн: начинаем с Волны 0–1? Словарь (Волна 6) — параллельно или потом?
5. (закрыто 2026-09-24) Нарушения границы: ссылки Новостей оставлены как исключение,
   ссылка в навигации Циклов убрана.
6. Когда включать «Фан-клуб» в меню главной (Волна 1 или позже)?
7. (закрыто 2026-09-25) `Archive/`: шапочные ссылки убраны, три ссылки в тексте — в `ALLOW`.
